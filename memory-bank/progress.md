# PtIDLE 执行进度记录 (Progress)

> 此文件由 AI 在项目开发过程中逐步填充
> 记录每个任务的执行状态、完成时间、遇到的问题等

---

## 待开发

| 任务ID | 名称 | 备注 |
|--------|------|------|
<!-- T-FOLLOW-9 已完成 (2026-08-07), 见「已完成」表. 其余 UptimeRobot/5xx 告警阻塞于域名 -->

---

## 开发中

<!-- 在此记录正在开发的任务 -->

---

## 已完成

| 任务ID | 名称 | 完成时间 |
|--------|------|----------|
| T001 | 初始化 Node.js 项目结构 | 2026-03-10 |
| T002 | 配置 TypeScript 和 ESLint | 2026-03-10 |
| T003 | PostgreSQL 数据库设计 | 2026-03-10 |
| T004 | Redis 配置 | 2026-03-10 |
| T005 | 用户注册 API | 2026-03-11 |
| T006 | 用户登录 API | 2026-03-11 |
| T007 | JWT 认证中间件 | 2026-03-11 |
| T008 | 玩家初始化逻辑 | 2026-03-11 |
| T009 | 获取玩家数据 API | 2026-03-11 |
| T010 | 离线收益计算服务 | 2026-03-11 |
| T011 | 离线收益结算 API | 2026-03-11 |
| T012 | 采集技能数据模型 | 2026-03-12 |
| T013 | 采集 API（开始采集） | 2026-03-11 |
| T014 | 采集进度查询 API | 2026-03-11 |
| T015 | 采集完成与收益计算 | 2026-03-11 |
| T016 | 加工配方数据模型与服务层 | 2026-03-12 |
| T017 | 加工 API | 2026-03-12 |
| T018 | 仓库资源查询 API | 2026-03-12 |
| T019 | 仓储上限管理 | 2026-03-12 |
| T020 | 制造配方数据模型 | 2026-03-19 |
| T021 | 实现卡牌制造 API | 2026-03-19 |
| T022 | 实现装备制造 API | 2026-03-19 |
| T023 | 实现消耗品制造 API | 2026-03-19 |
| T024 | 实现生产装备效率计算 | 2026-03-19 |
| T025 | 实现周期性挂机收益计算 | 2026-03-19 |
| T026 | 实现职业数据模型服务层 | 2026-03-19 |
| T027 | 实现棋子创建 API | 2026-03-19 |
| T028 | 实现棋子查询 API | 2026-03-19 |
| T029 | 实现棋子命名 API | 2026-03-19 |
| T030 | 定义基础卡牌数据模型 | 2026-03-19 |
| T031 | 实现卡牌库查询 API | 2026-03-19 |
| T032 | 实现卡牌分配 API | 2026-03-19 |
| T033 | 实现棋盘初始化逻辑 | 2026-03-19 |
| T034 | 实现移动判定逻辑 | 2026-03-19 |
| T035 | 实现攻击判定逻辑 | 2026-06-09 |
| T036 | 实现回合流程控制 | 2026-06-09 |
| T037 | 实现手牌抽牌逻辑 | 2026-06-10 |
| T038 | 实现回合结束保留 1 张手牌 | 2026-06-10 |
| T039 | 实现战士职业逻辑（权限+攻击累计+嘲讽+状态栏数据层） | 2026-06-10 |
| T1001 | 实现战棋公共池系统（补足手牌 + 不可保留 + 不计入职业机制） | 2026-06-10 |
| T040 | 实现弓手职业机制 1（攻击累计增伤，damage_boost 状态效果） | 2026-06-10 |
| T041 | 实现法师职业机制 2（debuff/灼伤系统，fire mark + burn DoT） | 2026-06-11 |
| T042 | 实现匹配队列 API (POST /api/match/queue) | 2026-06-11 |
| T043 | 实现匹配状态查询 + 取消匹配 API (GET + DELETE /api/match/queue) | 2026-06-11 |
| T044 | 实现对手匹配逻辑（tryMatch + Lua 撮合 + battles 行创建 + LOSER 兜底） | 2026-06-11 |
| T045 | 配置 Socket.io 基础连接（HTTP+WS 同 server + 握手期 JWT 鉴权 + socket.data 元数据） | 2026-06-11 |
| T046 | 实现房间管理逻辑（user-room 推送通道 + battle:join 鉴权 + 撮合 push + 断线通知） | 2026-06-11 |
| T047 | 实现实时状态同步（broadcaster 函数库 + battle:join 初期推 full state） | 2026-06-11 |
| T048 | 实现战场初始化（双 join 触发 + 7 步流水 + 失败回滚） | 2026-06-15 |
| T049 | 实现移动操作同步（battle:move + executeMove 流水 + board 广播 + phase 推进） | 2026-06-15 |
| T050 | 实现打牌操作同步（attack + tactical taunt 17 步流水 + 副作用 + 广播 + 阶段推进） | 2026-06-17 |
| T051 | 实现回合切换 orchestrator（executeEndStep 11 步 + executeRoundEnd 5 步 + tickBurnDamageOnTarget + battle:skip_play 事件） | 2026-06-17 |
| T052 | 实现胜负判定逻辑（kill star + base star + 6 阈值胜利 + 平局 + DB 持久化 + WS 广播） | 2026-06-17 |
| T053 | 实现卡牌消耗（consumePlayerCard + 步骤 9.5 + withTransaction 事务） | 2026-06-18 |
| T054 | 实现对战结算 API（POST /api/battle/result + 玩家 wins/losses/draws 累加 + player_battle_history + Redis 清理 + 幂等） | 2026-06-20 |
| T055 | 操作合法性校验中心化（WS Handler 入口跨切校验：room membership + battle status + rate-limit via Redis Lua） | 2026-06-20 |
| T-FOLLOW-1 | 实现 migrations runner 自动化（`npm run db:migrate` 脚本 + `schema_migrations` 跟踪表 + idempotent 事务 + 启动顺序） | 2026-06-20 |
| T-FOLLOW-2 | Migrations 启动期集成 + README 文档（`checkMigrationsStatus` 只读检测 + `index.ts` 启动 warn + 根 + backend 双 README） | 2026-06-20 |
| T-FOLLOW-3 | CI/CD 接入（GitHub Actions ci.yml + PG 16 / Redis 7 service containers + 42/701 jest 全量 + coverage artifact + README badge） | 2026-06-20 |
| 测试基线 | T-FOLLOW-3 收尾：CI workflow 文件就绪，待 push 后首次跑通验证（本地 42/701 仍全绿） | 2026-06-20 |
| CI 首次跑通 | T-FOLLOW-4 commit 5582977 推送后, GitHub Actions CI 首次跑通 (run #27936624591): 2 min 17 sec, 14/14 steps success, jest 42/701 + db:migrate + coverage artifact 全过 | 2026-06-22 |
| v0.1.0 Release | T-FOLLOW-4 release workflow 首次跑通: 推送 tag v0.1.0 (commit 634e2ee) → run #27937392708, 3 min 19 sec, 6/6 steps success → GHCR 4 tag 上传 (latest / 0.1 / 0.1.0 / 634e2ee)。Multi-arch (linux/amd64+arm64) build 无失败但需 make public 后 docker manifest 验证。**默认 private** — 需用户手动在 package settings 改 public | 2026-06-22 |
| T-FOLLOW-4 | CD 接入 - 镜像层（Dockerfile multi-stage + .dockerignore + release.yml multi-arch GHCR + docs/deploy.md + 本地 smoke test 通过 /health 200） | 2026-06-22 |
| T-FOLLOW-5 | 单 VPS 部署编排（migrate.js 重写 + Dockerfile baked migrations + docker-compose 4 services + deploy.yml workflow_run trigger + scripts/deploy.sh + docs/deploy.md § 5.3 + memory-bank 同步） | 2026-06-22 |
| T-FOLLOW-6 | HTTPS / TLS / domain（Caddy 2-alpine 第 5 service + Let's Encrypt HTTP-01 + caddy_data 持久化 + 删 backend host port + .env 加 DOMAIN/ACME_EMAIL + docs/deploy.md § 5.3 DNS 步骤） | 2026-06-22 |
| T-FOLLOW-6 bug fix | CI Run #28099535056 失败修复 - migrate.js 内联 pg.Pool 移除 .ts 依赖（CI 不 build；npm run db:migrate 直接 require .ts 抛 MODULE_NOT_FOUND）。**CI 验证**: commit 34a1625 推送后 Run #28113095056 全 13 步 success (2m17s) | 2026-06-25 |
| T-FOLLOW-7 | 自动回滚（deploy.sh 加 .last_good + health check fail 时 restore 旧 image + docker-compose ${BACKEND_IMAGE} env var 模式） | 2026-06-25 |
| T-FOLLOW-7 v0.1.1 部署 | tag v0.1.1 推送 → Release #2 (run #28175386357) image build success → Deploy #1 (run #28175599367) 失败: job 8s exit 1, "Process completed with exit code 1"。根因待 SSH 调试 (`bash -x scripts/deploy.sh 2>&1 | tee /tmp/deploy-debug.log`)。生产 backend 仍是 v0.1.0 image | 2026-06-26 |
| T-FIX 质量修复批次 | 修复已完成代码的 P0 级 bug（详见 history 2026-08-06 条目）：双状态源、手牌类型冲突、回合轮转、阵营判定、据点坐标、skip_play 归属、AOE currentRound、撮合 SQL 优先级、制造扣料顺序、采集/离线/加工幂等、IDOR、JWT 密钥、health 探测、deploy.sh 回滚 | 2026-08-06 |
| T-FIX 批次 2 (P1/P2) | 并发/资产安全 + 代码整洁：moveCharacter Lua 原子、settleBattle 行锁幂等、consumePlayerCard 归属复核、全局错误中间件、CORS 收敛 + auth 限流、共享缓存工具、Redis key 常量集中、错误码统一、迁移 004 + 唯一约束 | 2026-08-06 |
| T-FOLLOW-8 | 备份策略（daily pg_dump + 保留 daily14/weekly8 + 恢复 + storage 抽象） | 2026-08-06 |
| T-FOLLOW-7 失败诊断 | deploy.sh 加 ERR trap (`set -E` + `trap on_error ERR`) 打印失败行号 + 定位提示，改善 v0.1.1 部署失败 (8s exit 1) 无 step-level 输出问题。裸命令失败 (cd/pull/migrate/up) 触发，`if`/`while` 不触发。本地 bash 模拟验证 trap 行为；仍需 VPS 访问真正修复部署 | 2026-08-06 |
| T-FOLLOW-9 | 监控（GH Actions scheduled health check：每 15 min curl /health，失败开/评论 issue 告警，恢复自动关 issue；复用 VPS_HOST，GITHUB_TOKEN 开 issue） | 2026-08-07 |
| v0.1.2 Release | tag v0.1.2(4824672) -> release.yml run 31161641510 (首次冷构建 multi-arch 29min 被取消, rerun 吃 cache 4min success) -> deploy.yml run 31164125069 (workflow_run) 自动 success. 上线 e7e51c9 (T-FIX P0+资产安全+/health probe). /health 现返回 database:ok redis:ok (#5 修复). 镜像滞后解决 | 2026-08-07 |
| P2 代码改进 批次1 | 注册事务化 + 响应包裹统一。createUser 包 withTransaction（existence+INSERT user+initializePlayer 原子化，修孤立 user bug；initializePlayer 加可选 client），新增 utils/http.ts ok()/fail()，12 routes+4 controllers 全部信封化（裸数据->ok，错误加 success:false），全局错误中间件加 success:false。tsc 零错；jest 703/703 全绿（+1 注册回滚回归测试）；真库 smoke 验证 commit/duplicate/ROLLBACK。剩 zod 字段校验 + REST 统一(next(error)/ApiError) 下一批 | 2026-08-07 |
| P2 代码改进 批次2 | zod 字段校验 + REST 统一。新增 zod@3 + `middleware/validate.ts`（safeParse 失败 next(ApiError(400,首条msg))，通过替换 req.body）+ `validations/` 6 schema（auth/gathering/crafting/processing/characters/battle，自定义 message 对齐既有契约）；新增 `utils/ApiError.ts`（status+code?+extra?）+ `middleware/errorHandler.ts`（状态感知：ApiError 按 status+展开 extra / ZodError 400 / 其余 500 屏蔽）；路由/控制器 catch 统一 next(error)，ad-hoc `Error&{code}` 收敛为 ApiError（gathering 已活跃/processing 缺料+玩家不存在，缺料 missing 经 extra 回传），catch-all 500 不再各自 console.error+fail；auth/rateLimit 401/429 补 success:false。matchmaking LOSER 兜底保留原结构。tsc 零错；jest 703/703 全绿；10 个集成测试 app 挂载 errorHandler 保持错误信封一致。路由命名审计：现状一致（action 风格 /start /complete /result 等有意为之），无需改名 | 2026-08-07 |
| v0.1.3 Release | tag v0.1.3(a2514ba) push → Release run 31262817512 success（含 P2 批次1&2 后端改进 + 前端骨架仓库提交，前端不进镜像）→ Deploy run 31262952269 success 自动上线。health-check 连续 5 次 success 验证 VPS 在线。T-FOLLOW-7 部署问题确认已解决 | 2026-08-08 |
| T057 | 初始化 Vue 3 项目（Vite5 + Vue3 + TS + Router + Pinia + axios + socket.io-client）。含类型定义对齐后端契约、axios 拦截器(JWT+401登出+信封剥离)、3 stores(auth/player/game)、路由守卫、登录/注册/主页+离线收益弹窗、5 占位视图。`npm run build` 通过（vue-tsc+vite）。T058/T059 同步完成（路由+Pinia 随骨架就绪） | 2026-08-08 |
| T058-T063 | 随 T057 骨架一并完成（代码已实现，此处补登）：T058 路由(router/index.ts 7 路由+懒加载+beforeEach 守卫)/T059 Pinia(auth/player/game 3 store)/T060 登录页/T061 注册页/T062 主界面布局(HomeLayout 顶栏导航)/T063 离线收益弹窗(HomeView modal) | 2026-08-08 |
| T064 | 采集界面。新增 stores/gathering.ts(skills/efficiency/activeTask+loadAll/start/complete/cancel，complete 成功后刷 player profile)+ components/GatheringPanel.vue(技能列表+活跃任务进度条+领取/取消+2s 轮询检测后端定时器自动完成)+ utils/resources.ts(资源名映射)+ WorkshopView 改 tab 壳(采集/加工/制造，加工/制造占位留 T065/T066)。后端 T013/T014 已就绪无改动。npm run build + typecheck 通过 | 2026-08-08 |
| T065 | 加工界面。新增 stores/processing.ts(recipes/loadAll/process，process 成功刷 player profile；400 缺料取 lastMissing+刷 profile)+ components/ProcessingPanel.vue(配方卡 grid + input->output 流 + 数量 1/5/10 + 客户端预算校验 canAfford/missingFor + 缺料禁用按钮 + notice)+ types/index.ts(ProcessResult 加 resources/materials)。WorkshopView 挂载替换占位。后端 T017 processing route 已修(commit 16af1fa: input 走 resources 校验/扣除而非 materials)。typecheck 零错；build 通过(WorkshopView chunk 9.69kB)；API smoke 全过(冶炼/木工/研磨×1+smelting×5+缺料 400 missing 顶层) | 2026-08-12 |
| T066 | 制造界面。新增 stores/crafting.ts(recipes/loadAll/craft，craft 按 category 分发 card/gear/consumable 端点，成功+失败都刷 profile；后端 result.success 模式无 missing 数组，错误仅文案)+ components/CraftingPanel.vue(3 分类 section: 卡牌/装备/消耗品 + 替代料 input「或」连接 + 数量 1/5/10[装备强制1] + 客户端预算校验 canAfford/missingFor[任一替代组合满足] + 职业门槛 badge/禁用 + notice)。WorkshopView 挂载替换占位(工坊三子页全完成)。后端 T021/T022/T023 已就绪无改动。typecheck 零错；build 通过(WorkshopView chunk 14.55kB)；API smoke 全过(card/gear/consumable×1+法师火球卡×3缩放+弓手职业卡+回血药替代料+缺料400无missing+职业403+卡牌上限400边界) | 2026-08-12 |
| T067 | 仓库界面。改 frontend/src/views/WarehouseView.vue(替换占位)：onMount 调 player.fetchWarehouse(GET /warehouse)+ 资源/材料两 section + 分类用量条(绿<80%/黄<100%/红>=100%，storageLimits={resource:1000,material:500,gear:50} 分类级总和上限)+ 物品 grid(按数量降序，0 值 dim)+ 刷新按钮+ 本地 loading/error。后端 T018 已就绪无改动(player store 已有 warehouse ref+fetchWarehouse)。typecheck 零错；build 通过(WarehouseView 0.25kB->3.34kB)；API smoke 全过(GET /warehouse 返回 resources/materials/storageLimits 字段+数据正确+401 未鉴权+UI 用量计算 资源510/1000 51%/材料173/500 35%) | 2026-08-12 |
| T068 | 战棋棋盘渲染。新增 frontend/src/components/BattleBoard.vue(9x9 CSS Grid 棋盘 + 基地(2,2/6,6)按 bases 染色 p1蓝/p2红/neutral虚线 + 状态条 round/step/phase/stars + 坐标轴 x0-8/y0-8 + cell-click 事件供 T071)+ 改 frontend/src/views/BattleView.vue(接 game store.board，WS 未接前预览 mock BoardStateEvent 验证渲染)。坐标约定: P1 侧 y=0(底)/P2 侧 y=8(顶)，行渲染 y8->y0。渲染技术 CSS Grid(非计划 Canvas/SVG: 离散格子+点击交互+主题一致)。game store 已完整(board/ownHand/move/playCard 全就绪)。typecheck 零错；build 通过(BattleView 3.28kB)。棋盘无 REST 状态端点(实时走 WS T073)，契约编译期对齐 BoardStateEvent(与后端 battleStateBroadcaster 核实一致) | 2026-08-12 |
| T069 | 战棋棋子渲染。新增 frontend/src/components/BattlePiece.vue(presentational 棋子: 职业单字 战/弓/法+职业色 --warrior/--ranger/--mage + 血量条按比例绿>50%/黄>25%/红<=25% + 护盾段拼接+🛡N 徽章 + 能量 pips + 状态效果点 boost/mark/burn/taunt + 当前行动者发光环 + 敌我边框 own蓝accent/enemy红danger + click emit 供 T071)+ 改 BattleBoard.vue(加 ownCharacterIds prop + pieceMap 位置->棋子映射 仅 alive+有位置 + 格子内渲染 BattlePiece + piece-click emit)+ 改 BattleView.vue(传 ownIds computed: preview?mockOwnIds:game.myCharacterIds + 充实 mock 6 棋子 own3 P1底(6,0)(7,0)(8,0)+enemy3 P2顶(0,8)(1,8)(2,8) 含受损 ranger3/8+护盾 warrior🛡2+被嘲讽+burn+mark_fire+当前行动者=own warrior 验证各分支)。CharacterStatus 无 side 字段敌我靠 myCharacterIds 区分。typecheck 零错；build 通过(BattleView 3.28kB->7.71kB, BattleView CSS 5.26kB)；dev server HTTP 200 | 2026-08-13 |
| 据点对称调整 (T068/T069 后续) | P1 基地 (3,3)->(2,2)，P2 保持 (6,6)，关于棋盘中心 (4,4) 严格对称(用户视觉验证后要求)。全栈改动: 后端 battleOutcomeService(BASES 数组+默认 bases JSON)/battleInitializationService/battleStateBroadcaster(类型+默认+广播参数 4 处) + 后端 3 测试(battleOutcomeService 5 占领场景+BASES+beforeEach / battleStateBroadcaster 7 处 / battleActionService mock bases) + 前端 types/index.ts(BoardStateEvent.bases 键)/BattleBoard.vue(isBase+baseSideAt+注释)/BattleView.vue(mock bases)。几何: 两据点 Chebyshev 半径 2 范围仅 (4,4) 单格重叠，场景 3 计数 3/0->2/0(结果不变)。默认棋子起始位均在安全区无需动。jest 战棋 3 suite 81/81 全绿 + tsc 零错 + 前端 typecheck/build 通过。预存 3 suite/28 test 失败(auth/socket)经 stash 验证为环境问题非本次引入 | 2026-08-13 |
| T070 | 战棋手牌渲染。新增 frontend/src/utils/cards.ts(CARD_TYPE_META attack红/defense蓝/tactical绿 + effectSummary 把 effect JSONB 解析中文摘要: 伤害/范围/AOE/护盾/治疗/移动/嘲讽, 对齐后端 7+1 卡 effect 结构, HandCard 运行时无 description 故派生)+ 新增 frontend/src/components/BattleHand.vue(presentational: 角色头标 职业字+名+能量+行动中标签 + 手牌横排卡牌 类型色边框+⚡费用徽章+来源徽章 牌库/公共池[warning色] + 卡名+类型标签+效果摘要 + 可出牌判定 isCurrentActor&&isPlayPhase&&cost<=energy 否则 dim/禁用, 能量不足卡 unaffordable 半透明 + card-click emit {characterId,card} 供 T072)+ 改 BattleView.vue(板下手牌区 panel, 每个 own 角色一组从 board.characters 取 name/profession/energy + handGroups computed 合并 displayHand + 预览 mock ownHand 3 角色 8 卡型全覆盖[含 public_pool 轻击+能量不足战士重击 cost2>energy1] + 预览阶段切换 move<->play 按钮验证可点击态 + card-click 占位 console.log)。ownHand 来源: preview?mockOwnHand:game.ownHand(WS battle:state:full/hand 推送, T073 接入)。typecheck 零错；build 通过(BattleView 7.71kB->12.04kB, CSS 5.26kB->7.99kB)；dev server HTTP 200(root/BattleHand.vue/BattleView.vue/cards.ts transform 全 200) | 2026-08-13 |
| T071 | 战棋移动交互。**后端**: characterStatusService.ts CharacterStatus 加 movement 字段(interface+baseInfo+DB SELECT movement+return; piece.movement 已在 Redis piece/DB, getCharacterStatus 之前未广播) + characterStatusService.test.ts mockPiece 加 movement:2 + expect(status!.movement).toBe(2) 断言。**前端**: 新增 utils/movement.ts(computeReachableCells BFS 4 方向无对角, maxDistance=movement, 阻塞=occupied 含死棋, 起点穿透, 返回可达格 key 集合不含起点; 对齐后端 bfsFindReachablePositions; 客户端范围仅 UX 提示服务端 validateMovement 再校验)+ 改 types/index.ts(CharacterStatus 加 movement)+ 改 BattleBoard.vue(movableCells+selectedCharacterId prop, cell.movable 高亮 success 色+中心点::after, BattlePiece 传 is-selected)+ 改 BattlePiece.vue(isSelected prop + .selected 虚线脉冲环 animation)+ 改 BattleView.vue(selectedCharacterId ref + canSelectActor computed[move 阶段+当前 actor 是己方] + movableCells computed[BFS from currentActor.position+movement, occupied=所有有位置棋子同源 getAllBoardPositions] + onPieceClick toggle 选中 + onCellClick 执行移动[preview 更新 mockPositions/real 调 game.move] + watch actor|phase 字符串变化清选中 + mock 角色加 movement[warrior2/ranger3/mage2] + mockPositions reactive 支持预览交互移动 + 移动提示文案 + lastError 显示)。**关键**: getCharacterStatus.position 派生自 getAllBoardPositions, 故前端 occupied 用 position!=null 精确镜像后端阻塞(死棋死亡不移除位置)。typecheck 零错; build 通过(BattleView 12.04kB->13.99kB, CSS 7.99kB->9.04kB); 后端 tsc 零错 + jest 4 战棋 suite 134/134 全绿; dev server HTTP 200 | 2026-08-13 |
| T072 | 战棋打牌交互。**前端**: utils/cards.ts 加打牌分类(cardNeedsTarget[单体攻击/嘲讽]+cardIsAOE+cardSupported) + computeCardTargets(敌方+存活+射程内; 近战欧氏≤1.5/远程≤effect.range/嘲讽≤range??3, 对齐 validateAttack/validateTauntCard) + 改 BattleHand.vue(选中高亮 selectedCardDeckId prop + unsupported 卡[defense/heal/movement 后端 T050 unsupported_card_type]禁用+「暂不可用」标)+ 改 BattleBoard.vue(targetableCharacterIds prop + 传 is-targetable)+ 改 BattlePiece.vue(isTargetable prop + .targetable danger 准星脉冲环 target-pulse)+ 改 BattleView.vue(打牌状态机: canPlayCards[play 阶段+当前 actor 己方] + selectedCard ref + targetableIds computed + onCardClick[AOE 直接 playCard/needsTarget 进目标模式, 再点同卡取消] + onPieceClick[目标模式优先: 点可目标棋子 playCard+targetId/点其他取消] + onCellClick[目标模式点空取消] + 跳过出牌 game.skipPlay + watch actor\|phase 清 selectedCard/selectedCharacterId/previewNotice + 预览 previewNotice 反馈 + 提示文案)。**关键**: defense/heal/movement 后端 T050 不支持故前端禁用; AOE 无需选目标自动命中射程内全部; 嘲讽强制目标由服务端 getTauntRedirect 强制客户端不建模(回 battle:play_card:error 优雅降级); 客户端射程仅 UX 提示服务端再校验。无后端改动。typecheck 零错; build 通过(BattleView 13.99kB->16.57kB, CSS 9.04->10.15kB); dev server HTTP 200(root/BattleView.vue/cards.ts transform 全 200) | 2026-08-14 |
| T073 | WebSocket 对战连接。改 frontend/src/stores/game.ts(battle:matched handler 加 joinBattle(payload.battleId) 自动加入对战房间 -- 补齐 T057 骨架连接层的最后缺口; connect()/disconnect() + 全部 battle:state:full/board/session/hand/character/bases + battle:opponent_joined/disconnected + battle:end + battle:move/play_card/skip_play/join:error handler 早已就绪于 T057)。matched=true 时后端 emit battle:matched -> handler 复位 matching/inQueue + 自动 emit battle:join -> 后端回 battle:join:ok + 推 battle:state:full(棋盘+ownHand+myCharacterIds 由 ownHand keys 推断)。**e2e 验证(本会话)**: node 脚本(ptidle_default 网络 node:20 容器挂载 host backend/node_modules) 2 客户端(register/login+socket.io-client+matchApi) -> A 入队 matched=false -> B 入队 matched=true -> 双端收 battle:matched -> auto-join -> 双端收 battle:join:ok + battle:state:full(6 棋子/3 手牌/bases/状态同步) ✅ PASS。typecheck 零错; build 通过 | 2026-08-14 |
| T077 | 匹配队列界面。改 game.ts(加 queueMatch/cancelMatch 并导出: queueMatch 调 matchApi.join 设 matching+inQueue=true, matched=true 依赖 WS handler(T073) auto-join 不在此复位 inQueue 防止 WS 事件未到时 UI 闪烁回「开始匹配」, 失败回 lastError; cancelMatch 调 matchApi.leave 复位 matching/inQueue/matched) + 改 BattleView.vue(替换 T077 占位为匹配面板: game.matched->「已匹配到对手，正在进入战斗」/ game.inQueue->「匹配中，等待对手加入」+取消按钮调 onCancelMatch->game.cancelMatch / 空闲->「开始匹配」按钮调 onStartMatch->game.queueMatch; 预览(开发)按钮匹配中隐藏 v-if=!inQueue&&!matched) + .match-panel/.match-status/.matching/.match-idle CSS。**e2e 验证(本会话 同 T073)**: 匹配 REST + WS auto-join + 状态同步全流程通过。typecheck 零错; build 通过(BattleView 16.57kB->17.07kB) | 2026-08-14 |
| T075 | 卡牌库界面。改 frontend/src/views/CardsView.vue(替换占位): onMounted 调 player.fetchMyCards(GET /cards/my/list, page/pageSize 默认1/50) + 类型筛选 tabs(全部/攻击/防御/战术, 客户端筛当前页 + 各类型计数 badge) + 卡牌 grid(每张: 类型色左边框 --card-color + 类型标签 CARD_TYPE_META + ⚡cost 徽章 + 卡名 + effectSummary 中文效果摘要 + ×N 数量徽章[quantity>1 才显] + 获得日期) + 分页(上一页/下一页 + 第X/Y页·共N张, totalPages>1 才显) + loading/error/empty 状态 + 刷新按钮。**数据层早已就绪**: cardApi.myList(services/api.ts) + player store myCards/cardPagination/fetchMyCards + types PlayerCard/Pagination 均在 T066 制造卡牌时预建, 本任务纯 UI。复用 utils/cards.ts CARD_TYPE_META+effectSummary(T070 已验证渲染)。对齐 WarehouseView UI 模式/CSS 变量。typecheck 零错; build 通过(CardsView 3.42kB JS+2.01kB CSS)。**API smoke**: 容器内 node fetch 注册->登录->cards/my/list 未鉴权 401 + 鉴权 200 success data=[]+pagination{page,pageSize,total,totalPages} 字段完整。**工具显示坑**: Read/grep 把 api.ts 模板字符串内的 id 插值渲染成含模型名(glm_5.2)的乱码串, hexdump 验证文件实际字节(24 7b 69 64 7d)即 id 插值正确无误, 系工具显示层替换非代码 bug, 切勿据显示误改 api.ts | 2026-08-14 |
| T076 | 棋子管理界面。**player store 加方法**: createCharacter/renameCharacter(成功更新 characters 列表对应项)/fetchDeck(id)->DeckCard[]/assignCard/removeCard (characterApi 全就绪于 T066 前后)。**改 frontend/src/views/CharactersView.vue(替换占位)**: 棋子列表 grid(职业色标签 warrior红/ranger绿/mage蓝 + 名字 + ❤️health/max_health/⚡energy/max_energy/👣movement + 阵亡标记, 选中高亮) + 创建棋子 inline 表单(名字 input+职业 select, characters.length<CHAR_MAX=9 才显创建按钮) + 选中棋子 inline 改名(点改名变输入框+确定/取消, enter 提交) + 牌组面板(DeckCard[] 最多 DECK_MAX=10, 每张类型色边框+效果摘要+移除按钮, 空态提示) + 可分配卡牌列表(cardApi.templates() 建 card_template_id->profession 映射, 前端复刻 canUseProfession[卡牌无 profession 或 'common' 通用, 否则须严格匹配棋子 profession]预筛 + 排除已分配给该棋子的 card_id, 牌库满禁用分配, 无卡/无匹配职业提示)。Promise.all 并行拉 characters+templates(+myCards 若空), selectedId watch 触发 loadDeck, 默认选中第一个棋子。复用 utils/cards.ts CARD_TYPE_META+effectSummary。对齐 WarehouseView/T075 UI 模式/CSS 变量。typecheck 零错; build 通过(CharactersView 6.97kB JS+3.97kB CSS)。**API smoke**: 容器内 node fetch 注册->登录->GET /characters=3 初始棋子(profs warrior,ranger,mage 验证 initializePlayer) + PUT /:id/name 改名 200 name=Hero + GET /:id/deck=[] + POST /characters 创建第4棋子 201 + PUT /:id/deck 坏卡Id 404 Card not found + GET /characters 未鉴权 401。**IDOR 200(期望403)**: 本地 compose backend 容器跑旧镜像(v0.1.0, 创建于 2026-06-24, 早于 e7e51c9 T-FIX IDOR 修复), 容器内 dist/routes/characters.js 无 Forbidden/owned.some 检查; 最新源码(已 push)+生产 v0.1.3 均已修复 IDOR, 非 T076 bug, 勿误改。分配/移除成功路径: 新用户无材料(POST /crafting/card 400 Insufficient materials)故本地未测, 由后端 characterService.test.ts 单测覆盖(assign/remove/职业校验/已分配/牌库满) | 2026-08-14 |
| T078 | 对战结算界面。**game store 改动**: 修复 myUserId 遗留缺口(connect 时 = auth.user?.id, 之前定义后从未赋值致胜负判定失效) + 加 settlement ref<SettlementResult|null> + fetchSettlement()(调 battleApi.result(battleId), 失败回 lastError) + battle:end handler 清 settlement(新结果重置旧) + resetBattle 清 settlement + return 暴露 settlement/fetchSettlement。**BattleView.vue 加结算面板**: v-if game.battleEnded 顶部优先展示(用 <template v-else> 包裹对战内容, 结算时隐藏棋盘/手牌/匹配面板/actions) + watch battleEnded 触发 fetchSettlement + 胜负 myResult(优先 settlement.yourResult, 降级 battleEnded.winnerUserId===myUserId; win/loss/draw) + 胜利方式中文(kill_threshold 击杀达标 / base_threshold 据点达标 / draw 平局) + 星数(我方 vs 对方, mySide 基于 p1UserId/p2UserId===myUserId 推断) + 时长(settlement.duration 格式 mm:ss) + 累计战绩(yourStats wins/losses/draws) + 返回大厅按钮(resetBattle 回匹配空闲态, 不断 WS)。**数据来源**: battle:end WS 推送 BattleEndEvent(基本: winner/victoryType/stars/userIds) + POST /battle/result 拉 SettlementResult(详细: yourResult/yourStats/duration); fetchSettlement 失败降级用 BattleEndEvent。**注意**: T078 上下文提「消耗的卡牌」但 SettlementResult 无单局卡牌消耗字段(PlayerStats 是累计胜负), 故按数据层支持展示胜负/方式/星数/时长/累计战绩。typecheck 零错; build 通过(BattleView 17.07kB->18.27kB JS, 10.15->11.07kB CSS)。**API smoke**: 容器内 node fetch POST /battle/result 假 battleId 404 {error:"Battle not found"} + 未鉴权 401。controller 错误映射确认: battle_not_found 404 / not_participant 403 / battle_not_finished 409 / success ok(data)。成功路径(返回 SettlementResult)需 finished battle 本地难造, 由后端 settleBattle 单测覆盖; 旧容器错误信封 {error}(无 success:false, 早于 P2 信封化) 前端 http 拦截器兼容 | 2026-08-14 |
| T079+T080 | API 对接 + JWT 管理(审视确认实质完成, 随各 view 落地, 无代码改动)。**T079**: services/api.ts 封装 12 个 api 对象(auth/player/gathering/match/processing/crafting/character/card/warehouse/profession/skill/battle) 覆盖所有前端 view 用到的后端端点; http.ts 响应拦截器统一信封剥离(response.data 返回 {success,data}) + 401 登出。3 个未用单详情端点(processing /recipes/:type, professions /:name, skills /gathering/:type)前端无 view 调用故未封装(非缺口, 避免死代码)。**T080**: token 存 localStorage(TOKEN_KEY/USER_KEY) + 请求拦截器自动附带 Bearer + 401 响应拦截 logout+跳 /login; 后端无 /refresh 端点故无 token 续期, "刷新"由 401 登出重登覆盖(当前后端能力下完整)。各 view 均经 store->api->http 链路, 无绕过 service 层直连 axios。对照后端 35 个路由端点逐一核验覆盖度 | 2026-08-14 |
| T081 | 集成测试与 Bug 修复。**测试基线**: 后端 jest 39/42 suite 676/691 test 通过; 3 suite/15 test 失败全为 ECONNREFUSED 127.0.0.1:5433(PG)/Redis 连接(socketServer.test 需 connectRedis / authController.test 需 pool.connect / wsValidation.integration 需真实 PG+Redis) -- compose 不发布 host 端口 + dev 容器(ptidle-dev-pg 5433/ptidle-dev-redis 6379)未运行致, 环境问题非代码(对齐 memory 已知)。**修复已知 bug①(gathering 主副颠倒)**: skillService.getGatheringConfig 用 Object.keys(yields)[0] 取主产物, 依赖 JSONB key 顺序; 实际 DB mining {"coal":0.3,"iron_ore":1}/woodcutting {"sap":0.2,"wood":1} 副产物排在前 -> 主副颠倒(采矿主得煤/伐木主得树液, byproductChance=1/0.3≈3.33>1 必出"副产物"); test mock 顺序主在前故 test 绿而生产颠倒。改为按 rate 最大者定主产物(不依赖 key 顺序), byproductChance 加 primaryYield>0 防 NaN。加防回归 test(副在前顺序断言 iron_ore/wood 仍为主)。skillService 8/8 通过; 全量 jest 676/691(+1 新增 test pass), 15 failed 不变(环境)。**审查无 bug**: 前端战棋链路(movement.ts BFS 4方向/起点穿透/阻塞/cards.ts 打牌分类+射程/BattleView.vue 移动+打牌+T078结算胜负判定+myUserId 修复)逻辑正确; gatheringService.calculateGatheringYield 修复后主产物 iron_ore/wood 入仓、副产物按 0.3/0.2 概率出。**已知非阻塞(记录不修)**: craftingService executeGearCrafting/executeConsumableCrafting 未事务化(consumable 扣料 :568 与发消耗品 :543-565 分两条 execute, 中间失败不一致; gear 校验 query 与写 execute TOCTOU 窗口), executeCardCrafting 已 withTransaction 修过顺序 bug; 单玩家并发 craft 概率低 + 本地无法跑集成测试验证(ECONNREFUSED), 留作后续 polish | 2026-08-14 |
| T082 | 性能优化(5 项全实现)。**①后端 HTTP 压缩**: `compression` 中间件(+依赖 `compression@^1.8.1`/`@types/compression`), 挂在 cors/json 之后路由之前, 按 Accept-Encoding 协商 gzip, API JSON(卡牌模板/玩家数据/战斗状态)传输体积显著降低; 新增 `src/middleware/compression.test.ts`(supertest 断言 Accept-Encoding: gzip 时 Content-Encoding: gzip / identity 时不压缩) 2/2 通过。**②前端 socket.io-client 延迟加载**: game store connect() 改 async + `await import('socket.io-client')`, 原静态 import 经 App.vue->game store 链路把整个 socket.io 子图拉进主 chunk, 首屏(登录/主页/工坊)被迫下载; 改动态 import 后拆独立懒加载 chunk, 主 chunk(eager) 197KB->156KB, socket.io ~44KB 仅进对战时下载(index.html 已确认不 preload)。**③vite manualChunks**: `build.rollupOptions.output.manualChunks` 仅把 vue 全家桶(vue/vue-router/pinia/@vue)拆 vendor-vue chunk(跨路由复用+长期缓存); 其余 node_modules 返回 undefined 交回 Vite 默认拆分--动态 import 的 socket.io-client 传递依赖(engine.io-client 等)自动成懒 chunk, 静态 import(axios)并入引用方; 不强行归类 socket.io 依赖否则会破坏延迟加载。**④后端优雅关闭**: SIGTERM/SIGINT handler(Docker stop/recreate 触发) 依序 io.close()(关 Socket.IO+底层 HTTP server 停新连接) -> pool.end()(PG) -> disconnectRedis(), 各步 try/catch 防连环失败, shuttingDown 幂等标志防重入。**⑤crafting 事务化(修 T081 已知③)**: executeGearCrafting/executeConsumableCrafting 改 `withTransaction` + 玩家行 `SELECT ... FOR UPDATE`(对齐 executeCardCrafting 且更强--读移入事务内); gear 修 TOCTOU(原事务外 read->校验->单 execute write, 并发扣料丢失更新), consumable 修扣料与发消耗品分两条 execute 中间失败不一致(PLAYER_NOT_FOUND/INSUFFICIENT 以 code 抛出经 withTransaction 统一 ROLLBACK); 配方读取(静态缓存数据)与 gear 类型校验留事务外提前失败。+3 防回归 test, craftingService 34/34 通过。**验证**: 后端 jest 40/43 suite 681/709(3 env-fail suite 不变=socketServer/authController/wsValidation.integration ECONNREFUSED, 环境非代码); 前端 build+typecheck 全过 | 2026-08-15 |

| T-FIX-DEADLOCK | 战棋 phase 状态机死锁修复（用户浏览器实测发现：卡「第1回合 步骤0 待机」）。**根因**: activateCurrentUnit(唯一 idle->draw 推进点)只在 executeEndStep 步骤 6.5 被调, 而其入口要求 phase∈{move,play}; initBattleField 初始化后与 executeRoundEnd 的 endCurrentRound 之后 phase 均停留 idle 且无人推进--战斗开始与每回合切换双双死锁(此前 e2e 仅验 matched->join->state:full, state:full 显示 phase=idle 未被识别)。**修复**: battleActionService 新增 activateActorForStep(io,battleId,{draw}) helper(activate->可选draw->completeDraw->双广播); initBattleField 步骤 6.5 激活首 actor(不抽牌, 初始手牌 init 步骤 4 已发); executeRoundEnd 步骤 7 仅无胜负时激活新回合首 actor(draw=true)。+4 回归测试; 全量 jest 43/43 suite 711/711(dev 容器已启故集成套件亦绿); tsc 零错 | 2026-08-18 |
| T-FIX-BATTLE3 | 用户报告三连 bug 修复(开局卡死/认输无效/求和无效, 双 socket E2E 复现定位): ①initBattleField 步骤 6 误用 query() 读 rowCount(恒 undefined)提前 return, 步骤 6.5 激活实为死代码 -> 改 execute(); ②tryInitBattleField init_lock 竞态(auto-join 双方并发 join, 后到者 SETNX 失败静默返回无人 init) -> 锁丢失轮询重试; ③broadcaster 仍用废弃 getDbSessionState 读 DB(phase 滞后) -> 改 Redis 主存 getSessionState; ④migration 011 漏改 battles.victory_type CHECK(011 误判无约束) -> migration 012 补 'surrender'; ⑤getPendingBattleForJoin 仅放行 pending, ongoing 对局刷新后永远 not_in_room -> 放行重连。认输/求和 handler catch 补日志。44/44 suite 723/723 全绿 + E2E 全链路验证(开局 phase=move/认输 surrender 落库/求和 accept->battle:end draw) | 2026-08-22 |
| T-BATTLE-INTERACT | 对战互动按钮：退出对战(认输)+请求平局。**后端**: migration 011(player_battle_history.victory_type CHECK 加 'surrender'); VictoryType/VictorySource 扩展(battleOutcomeService+battleSettlementService+broadcaster); 新 battleInteractionService(surrenderBattle/requestDraw/respondDraw, result 风格+ApiError 404/403/409) -- 结算复用 recordVictory(UPDATE battles+finishSession+broadcastBattleEnd), 战绩入账仍由前端收 battle:end 后 POST /battle/result 完成; 认输兼容 pending(卡死对局逃生门), 求和仅 ongoing; draw_request 存 Redis battle:{id}:draw_request(无 TTL, recordVictory 终局漏斗统一清理); 拒绝仅向请求方 user:{userId} 房间单播 battle:draw_declined; WS 3 事件(battle:surrender/draw_request/draw_response, 认输手动组合 checkRoomMembership+checkRateLimit 因需兼容 pending)。**前端**: game store pendingDrawRequest/drawRequestSent 状态+surrender/requestDraw/respondDraw emit+draw_requested(忽略自己)/draw_declined handler; BattleView 实战中显示 [请求平局](发出后禁用"等待对方回应…")+[退出对战](danger) 按钮, 两个自制 modal(退出二次确认"退出将判负"/对方求和"接受/拒绝"), VICTORY_LABEL 加 surrender:'退出判负'。+12 单测, 全量 44/44 suite 723/723; tsc/vue-tsc/build 全过 | 2026-08-18 |
| T1010 | migration 013: `battles.current_phase` CHECK 约束加 `'deployment'`(DROP+RE-ADD `battles_current_phase_check`), 列 COMMENT 同步更新。为 T1010 系列布置阶段铺路(布置期 UPDATE battles SET current_phase='deployment' 此前会被 CHECK 拒绝)。dev DB 已应用并实证约束含 8 值; 全量 jest 44/44 suite 723/723 绿 | 2026-08-23 |
| T1011 | deploymentService(布置核心逻辑): Redis `battle:{id}:deployment` 状态(双方草稿/confirmed/deadline/finalized 幂等标记)+ 服务端权威校验(结构: 恰好3棋子/摆位x范围/不重叠; DB: 棋子归属存活/卡牌归属/职业匹配(无模板视为common)/同名卡(同template_no,无模板按name)≤3/总张数≤12/一卡实例禁入多棋子卡组)+ 默认配置生成(前3 alive+职业固定位 AUTO_PLACEMENT[P1 法(0,0)猎(1,0)战(2,0)/P2 法(8,8)猎(7,8)战(6,8)]+character_deck 默认卡组, 固定位被占顺序兜底)+ finalizeDeployment(已确认方草稿/未确认方默认, 结果落 battles.battle_data 审计)+ getDeploymentView(对手草稿完全不下发)。**并发模型**: createDraft/update/confirm/finalize 全部 load-modify-save, 统一 withWriteLock(NX+EX 5s 自愈锁+50ms×5 重试)串行化, 消除双方并行草稿互覆盖与 confirm/finalize 竞态; 校验在锁外(重DB查询), 锁内重读状态落盘防 TOCTOU。redisKey 加 deployment/deployWriteLock。+29 单测(智能 mock 按 SQL 内容路由+内存 Map 模拟 redis NX 语义), 全量 45/45 suite 752/752; tsc 零错 | 2026-08-23 |
| T1012 | initBattleField 拆分两段式 + 对局卡组快照。**新入口**: `initDeployment(io,battleId)`(initializeBoard + createDeployment + broadcastDeploymentState)与 `startBattle(io,battleId)`(finalizeDeployment 幂等 -> persistDeckSnapshots -> initBattleField(io,battleId,deployedPieces) -> cleanupDeployment); tryInitBattleField 尚未切换(T1013 接 socket 事件时切, 保证每个 commit E2E 可用)。**initBattleField 兼容扩展**: 可选 deployedPieces 参数, loadBattleCharacters 拆 loadSideCharacters(ids 给定按配置顺序批量取+重排, 缺省前3 alive 旧口径), positionList 按配置放置; 无参调用行为完全不变。**卡组快照**: persistDeckSnapshots 把 finalize 结果按配置顺序写 Redis `battle:{id}:deck:{cid}`(deck_id 合成=card_id, 空卡组显式'[]', 库存缺失卡剔除), handService.drawCards 优先读快照(损坏 JSON 防御性回落 character_deck); broadcaster 新增 broadcastDeploymentState(battle:deploy_state 按用户视角单播, 对手细节隐藏)。+11 单测(快照优先级3/初始化两段式8), 全量 45/45 suite 763/763; tsc 零错 | 2026-08-23 |
| T1013 | 布置阶段 Socket 事件接线。**battleRoom 新增**: handleBattleDeployUpdate(草稿全量同步, 成功后 broadcastDeploymentState 双方视角)/handleBattleDeployConfirm(单方确认广播状态; 双方确认 -> startBattle, 失败回 start_failed 仅记日志--finalize 幂等可重试); checkDeployContext(房间成员+速率限制手动组合, 不用 validateOperationContext 因其仅放行 ongoing 而布置期 status=pending, 与认输同模式); isDraftLike 轻量预检挡异常 payload。**tryInitBattleField 切换**: 双方到齐+pending -> initDeployment(幂等, 重连重播 deploy_state)替代 initBattleField。**认输兼容布置期**: surrender 成功后 best-effort cleanupDeployment; 求和自然禁用(requestDraw 仅 ongoing)。socketServer 注册 battle:deploy_update/deploy_confirm。**E2E 冒烟通过**(dev 真实环境双 socket): 匹配->join->deploy_state(120s 倒计时)->非法草稿拒(details=placement_overlap)->合法草稿同步->p1 确认(opponentConfirmed 广播)->p2 确认->开战 phase=move->空卡组手牌走公共池; battle_data 部署审计落库+6 个 deck 快照+deployment key 清理。+8 单测, 全量 45/45 suite 771/771; tsc 零错 | 2026-08-23 |
---

## 问题与解决

| 日期 | 问题 | 解决方案 |
|------|------|----------|
| 2026-06-10 | `gathering.integration.test.ts` 3 个用例（开始采集×2、取消采集）因 `ClientClosedError: The client is closed` 失败。根因：集成测试未调用 `connectRedis()`，但 `idleQueueService.zAdd/zRem` 路径要求已连接的单例 | 在测试文件顶部加 `jest.mock('../config/redis', ...)` 覆盖所有用到的 Redis 方法。同步更新 `architecture.md` 增加「集成测试 Mock 模式」章节。详见 history 2026-06-10 条目 |
| 2026-06-18 | T052 收尾时 `npx jest` 跑出 7 个失败（5 `authController` + 2 `socketServer`）。根因是开发环境缺 PostgreSQL/Redis 服务（`ECONNREFUSED 127.0.0.1:5433` + `ClientClosedError`），不是代码问题 | `docker compose up -d` 启动 `ptidle-postgres-1`（5433）+ `ptidle-redis-1`（6379）；`socketServer.test.ts` 在 `beforeAll` 加幂等 `connectRedis()`（用 `redisClient.isOpen` 防重复连）；`afterAll` 不调 `disconnectRedis()` 防止关掉跨文件共享的单例。修复后 36 个 test suite / 620 个 test 全绿。同步更新 `architecture.md` Docker 配置 + 集成测试模式 章节。详见 history 2026-06-18 条目 |
| 2026-06-18 | T053 spec compliance 收尾时发现 T053 describe block 落在 executePlayCard describe 之外（结构问题）；code quality review 发现 partial delete 路径在 withTransaction 回调内显式 ROLLBACK 导致回调返回后 withTransaction 仍会 COMMIT，产生 driver-level 副作用 + 双日志 | (1) 修正 T053 describe 嵌套位置（amend 6dd68da→be85e9a，b1f3d61 保留原状）；(2) 重构 consumePlayerCard 用 PartialDeleteError sentinel 替代 in-callback ROLLBACK，外层 catch 用 instanceof 区分 partial (warn) vs error (error)，commit b42f89f。修复后 5/5 T053 + 23/23 executePlayCard + 480/480 service + 4/4 database 全绿 |
| 2026-06-20 | T054 simplify review 发现：(1) applySettlementInTransaction 4 个近重复调用（4a 双 UPDATE + 4b 双 INSERT）易引入参数错位 bug；(2) controller switch 缺 exhaustiveness 检查，新增 error variant 会静默漏分支；(3) integration/unit test 的 `jest.spyOn(console, 'error')` 在 describe 顶层 spy，永不 mockRestore 会污染同进程后续测试输出；(4) integration test 残留无用 `_refs = {...}` 占位语句 | (1) 把 4 个对称 SQL 调用改写为 `for side of [p1/p2]` 循环 + `insertBattleHistory` 收 1 个 `side` 对象替代 7 位置参数；(2) controller switch 加 `default: const _exhaustive: never = result.error` + throw，让新增 error variant 编译失败；(3) 两处 console.error spy 改用 `beforeEach` 局部 spy + `afterEach mockRestore()`；(4) 删 `_refs` 行。修复后 21/21 T054 + 39/39 全量 suite 全绿 |
| 2026-06-20 | T-FOLLOW-1 单测初次运行全部失败：`process.exit called with "1"` 立即终止 jest 进程，无任何 case 输出。根因 mockClient 缺 `release` 方法 → `applyMigration` `finally` 块 `client.release()` 抛 TypeError → 失败被 runMigrations 捕获 → `failureCount++` → 走 process.exit(1) | mockClient 加 `release: jest.fn()`。修复后 8/8 migrate test + 42/42 全量 suite 全绿 |
| 2026-06-20 | T-FOLLOW-2 smoke test 在 /tmp/ 写脚本跑 ts-node 失败 2 次：第一次相对路径解析失败（`./src/config/database` 在 /tmp/ 找不到），第二次 /tmp/ 文件被 ESM loader 拒绝（`ERR_UNKNOWN_FILE_EXTENSION`） | 把脚本挪到 `backend/src/scripts/smoke-warn.ts` 内部 + 用绝对路径 import。修复后 3 个状态切换（before 全 applied → drop 一行 → after hasPending=true → 恢复）全部正确 |
| 2026-06-20 | T-FOLLOW-3 端口混淆风险：dev docker-compose 把 PG 5432 → 5433（host 端口）避免本机冲突，但 CI GitHub Actions service container 内部就是 5432，**用 5433 会连不上** | workflow 显式 `DB_PORT: 5432` + `REDIS_PORT: 6379`，并加注释说明「dev = 5433 / CI = 5432」。`database.ts` 兜底 `|| '5432'` 兼容两端 |
| 2026-06-22 | T-FOLLOW-4 smoke test 容器内 `localhost` 解析问题：容器内 `localhost` = 容器自己 loopback，不是 host。直接用 `DB_HOST=localhost` 会连不上 host 上的 PG/Redis | `docker run --add-host=host.docker.internal:host-gateway` + `DB_HOST=host.docker.internal`（Docker 20.10+ Linux 支持）。文档化在 `docs/deploy.md` § 自定义 build |
| 2026-06-22 | T-FOLLOW-5 Task 1 spec 偏差: 实际 test 数 14 (8 已有 + 1 env var + 5 checkMigrationsStatus 边界), 不是 spec 写的 9; 需新增 `migrate.d.ts` 声明文件供 .ts 引用 | 实测 14/14, 接受偏差; `migrate.d.ts` 是 TS interop 标准做法, ts-jest + tsc 都需 |
| 2026-06-22 | T-FOLLOW-5 Task 2 spec 偏差: `tsc` 不编译 `.js` 文件, `dist/scripts/migrate.js` 不会自动生成 | Dockerfile 显式加 `COPY src/scripts/migrate.js /app/dist/scripts/migrate.js` |
| 2026-06-22 | T-FOLLOW-5 Task 4 code review 发现 bug: `docker compose up -d backend` 不加 `--force-recreate` 时, pull 新镜像不会被实际加载, 旧容器继续跑 | 改为 `docker compose up -d --force-recreate backend` (commit 52c625e) |
| 2026-06-22 | T-FOLLOW-5 Task 5 code review 发现 bug: `workflow_run` 触发时**不**自动 checkout, `script_path: scripts/deploy.sh` 找不到文件 | 加 `actions/checkout@v4` step, ref 用 `head_sha || github.sha` (commit eee5c29) |
| 2026-06-25 | T-FOLLOW-6 推送后 CI Run #28099535056 failed: "Apply database migrations" exit 1。根因：migrate.js (T-FOLLOW-5 commit 4f924e6 引入) 第一行 `require('../config/database')` 是 .ts 源文件，但 CI workflow 只 `npm ci` 不 `npm run build`，dist/ 没生成 → MODULE_NOT_FOUND。CI 自 T-FOLLOW-5 推送起就一直是失败状态，前 2 次 push 推送者未察觉 | 选 Option B：migrate.js 内联 pg.Pool + dotenv，让 .js 文件不依赖 .ts 编译产物；改 jest.mock 'pg' 替换 '../config/database'，bootstrap 走 pool.connect 调高各 case 计数（4/10/2）。修复后 14/14 migrate test 全绿，dist 不再需要 migrate.js 的 .ts 编译路径。Commit 0079eb2 (本地，待 push 验证 CI) |
| 2026-06-26 | T-FOLLOW-7 首次部署失败：v0.1.1 tag 推送后 Deploy workflow run #28175599367 失败，job 8s 内 exit 1（太短，跳过 health check 30s 窗口，失败必在 [1/6]~[3/6]）。`set -euo pipefail` 模式下任一前置命令失败即终止，错误信息仅 "Process completed with exit code 1"，无 step-level 输出（WebFetch 拿不到详细 log，需登录 GH UI）。最可能根因：(a) `docker compose pull backend` 拉不到 `:latest`（网络/认证问题，v0.1.0 部署时正常，可能 VPS 状态变化）；(b) `docker compose run --rm migrate` 启动后 migrate.js 报错；(c) `docker compose up -d --force-recreate backend` 容器启动失败。生产 backend 仍跑 v0.1.0，`.last_good` 仍未写入 | 临时：SSH 到 VPS 跑 `bash -x scripts/deploy.sh 2>&1 | tee /tmp/deploy-debug.log` 拿具体失败行；定位后修复并重 push tag (v0.1.2 或 amend v0.1.1)。长期改进：deploy.sh 在 `set -e` 模式下失败时打印 "FAIL at line N" 上下文；考虑分步 `|| true` 包裹非关键检查 (`.last_good` 读写) 让 deploy 状态可分 |

---

*此文件将在项目开始开发后逐步填充*
| T1014 | deadline sweeper(对局计时基础设施)。**新模块×2**(分层防循环依赖): ①battleDeadlineService(arm/clear 辅助, 常量 STEP_DURATION_MS=90s/SWEEP_INTERVAL_MS=1s/退避 10s; redisKey 加 stepDeadline[battle:{id}:step_deadline 含 step+actorId 的 JSON] + deadlineIndex[battle:deadline_index ZSET, score=到期ms, member=deploy:{id}/step:{id}]); ②deadlineSweeper(1s interval 扫 ZSET 到期条目: 布置到期->startBattle[前置校验 battles.status=pending+部署状态存在, 失败退避 10s 重试], 步时到期->认领[zScore 自检+ZREM]后比对记录 step/actor 与 session, 匹配才 executeEndStep[与手动 skip_play 同路径], 失配仅清旧记录; sweeping 标志防扫描堆积; 单条隔离 try/catch)。**接线**: createDeployment 武装布置索引/cleanupDeployment 清除; activateCurrentUnit(所有步骤激活唯一汇聚点: init 首步+executeEndStep 推进+executeRoundEnd 新回合首步)武装步时/finishSession+deleteSession 清除; startBattle 加 in-flight Set 守卫(sweeper 与双方确认并发只跑一次); index.ts 启动 startDeadlineSweeper+优雅关闭 stop。**broadcastSessionState/battle:state:session 加 stepDeadline 字段**(T1016 前端倒计时用); getCurrentState view 同步。**★顺手修 executeEndStep 既有 bug**: retain 盲取 hand[0], 纯公共池手牌(空卡组棋子常态)下 skip/超时推进必 retain_failed -> 改为优先保留首张非公共池卡, 全公共池传 null(全弃), 手动 skip_play 同样受益。**E2E 冒烟通过**(dev 双 socket, redis-cli 提前改索引/记录 deadline 触发): A 布置超时->sweeper 0.5s 内 startBattle(phase=move+stepDeadline 广播) B 步时超时->step 0->1 自动推进+新时限武装 C nodemon 重启->sweeper 从 Redis 恢复继续推进 step 1->2(顺带实测自愈分支: 只改索引不改记录会被拒发)。+24 单测(battleDeadlineService 7+sweeper 17, 内存 zset mock), 全量 47/47 suite 795/795; tsc 零错 | 2026-08-23 |
| T1015 | 前端布置界面(三面板)。**新组件 DeploymentPanel.vue**(~560 行): ①出战棋子选择(alive 卡片 grid, 恰好 3; 选中自动分配初始列 P1 左起/P2 右起; 已选卡再点一次切换摆位激活态) ②本方行 9 格摆位(点棋子激活+点格放置; 点己方已占格交换两棋子列位; y=0/8 随 mySide) ③每棋子配卡(tab 切换+职业过滤列表+加减张数; 同名≤3[键 t{no}/n{name} 对齐后端]/总≤12/一卡实例禁入多棋子卡组[copiesAvailable=quantity-本组已用, 他组已用>0 整卡禁用]; 空卡组合法走公共池); 120s 倒计时(deadline 本地 500ms tick, <10s 变红, 到期显示「等待自动开战」-推进由服务端 sweeper); 确认按钮(冲刷未同步草稿后立即 confirm, 同 socket 顺序送达); myConfirmed/finalized 锁定编辑。**草稿同步**: 深度 watch 节流 600ms 全量同步且**仅合法草稿上传**(中间态不传, 后端 updateDraft 只收合法全量草稿); lastSyncedJson 与 deploy_state.myDraft 对比采纳外部变更(刷新恢复/另一端编辑), 自身同步后的 echo 不回环。**客户端预检**与后端 validateDraft 同规则(双保险)。**数据源**: player store characters/myCards + cardApi.templates() 按 card_template_id join 得职业(无模板/无职业='common' 对齐后端 COALESCE)。**BattleView 接线**: deployState 非空渲染 DeploymentPanel; 布置期认输(退出对战)放行+求和按钮隐藏; 匹配等待面板布置期不再误显(v-else-if 加 !deployState)。浏览器双开验证通过(用户); vue-tsc 零错; build 通过(BattleView chunk 30KB 含面板) | 2026-08-24 |
| T1016 | 前端行动倒计时条。BattleView 加步时倒计时条: game.stepDeadline(battle:state:session 下发, T1014 已就绪)本地 500ms tick 渲染; 进度条(width=剩余/90s)+文字(本方行动/对方行动+mm:ss), 我方行动 accent 色/<15s danger 变红/到 0 显示「已超时, 等待服务器推进…」(推进由服务端 sweeper 负责, 前端不本地截断操作); 渲染条件=对局中+stepDeadline 非空+phase!=finished。vue-tsc 零错; build 通过(BattleView 30.86KB) | 2026-08-24 |
